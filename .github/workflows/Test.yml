name: Hello World!

on:
  push:
    branches:
      - master

jobs:
   Test:    
    runs-on: ubuntu-20.04
    steps:
      - name: Say testingo
        if: ${{ (! contains(github.event.head_commit.message, '#skips'))}} || ${{(github.event_name == 'push')  }} 
        run: |
          echo "Test"

 
